{
  "name": "classpass",
  "version": "1.0.0",
  "description": "A minimal class validator for basic javascript types",
  "main": "dist/app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "tsc && node dist/app.js",
    "start:auto": "nodemon --exec ./node_modules/.bin/ts-node -- src/app.ts"
  },
  "author": "Sorakthun Ly",
  "license": "ISC",
  "dependencies": {
    "reflect-metadata": "^0.1.10"
  },
  "devDependencies": {
    "@types/reflect-metadata": "0.0.5",
    "@types/express": "^4.0.35",
    "@types/node": "^7.0.12",
    "typescript": "^2.2.2",
  }
}
